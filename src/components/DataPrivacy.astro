---
// Data Privacy section with headline and 3 feature cards
---

<section class="py-20 bg-dark">
  <div class="container-custom">
    <div class="text-center mb-12">
      <div class="text-sm font-semibold text-primary">Complete Data Privacy</div>
      <h2 class="text-2xl md:text-3xl lg:text-4xl font-extrabold text-white mt-2">
        Your Data Stays Private and Never Trains AI Models
      </h2>
      <p class="text-gray-300 mt-3 max-w-3xl mx-auto">
        Requests are securely sent to model providers, processed, then deleted (never used for training).
      </p>
    </div>

    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      {[
        {
          title: 'Privacy Obsessed',
          desc: 'Your conversations never train public or private AI models.',
          icon: 'lock'
        },
        {
          title: 'Strict Invite‑Only Access',
          desc: 'Conversations are private and accessible only to invited team members.',
          icon: 'shield'
        },
        {
          title: 'Enterprise‑Grade Security',
          desc: 'Ideal for schools, healthcare, legal, finance, and enterprise teams handling sensitive data.',
          icon: 'check'
        }
      ].map((item) => (
        <div class="rounded-xl bg-dark-light border border-gray-800 p-6 shadow-md text-center">
          <div class="mx-auto mb-4 inline-flex h-12 w-12 items-center justify-center rounded-full bg-white/5 border border-white/10">
            {item.icon === 'lock' && (
              <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6">
                <path d="M7 10V7a5 5 0 0 1 10 0v3" stroke-linecap="round" stroke-linejoin="round"/>
                <rect x="4" y="10" width="16" height="10" rx="2" ry="2"/>
              </svg>
            )}
            {item.icon === 'shield' && (
              <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6">
                <path d="M12 3l7 4v5c0 5-3.5 9-7 9s-7-4-7-9V7l7-4Z" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            )}
            {item.icon === 'check' && (
              <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6">
                <path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            )}
          </div>
          <h3 class="text-white font-semibold text-lg">{item.title}</h3>
          <p class="text-gray-300 mt-2 text-sm">{item.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
